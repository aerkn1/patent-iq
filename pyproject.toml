[tool.poetry]
name = "patent-iq"
version = "0.1.0"
description = "Patent IQ - Intelligent Patent Analysis System"
authors = ["aerkn1 <your-email@example.com>"]
readme = "README.md"
packages = [{include = "patent_iq"}]

[tool.poetry.dependencies]
python = "^3.9"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
black = "^23.7.0"
isort = "^5.12.0"
flake8 = "^6.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
